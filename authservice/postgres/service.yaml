apiVersion: v1
kind: Service
metadata:
  name: authservice-postgres-headless
spec:
  clusterIP: None
  selector:
    app: authservice-postgres
  ports:
    - port: 5432
      targetPort: 5432

---

apiVersion: v1
kind: Service
metadata:
  name: authservice-postgres
spec:
  selector:
    app: authservice-postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP