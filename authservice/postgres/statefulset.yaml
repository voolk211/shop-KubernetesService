apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: authservice-postgres
spec:
  serviceName: authservice-postgres-headless
  replicas: 1
  selector:
    matchLabels:
      app: authservice-postgres
  template:
    metadata:
      labels:
        app: authservice-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: authservicedb
            - name: POSTGRES_USER
              value: user
            - name: POSTGRES_PASSWORD
              value: password
          volumeMounts:
            - name: authservice-storage
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: authservice-storage
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 500Mi
