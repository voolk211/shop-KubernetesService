apiVersion: v1
kind: Service
metadata:
  name: paymentservice-mongo-headless
spec:
  clusterIP: None
  selector:
    app: paymentservice-mongo
  ports:
    - port: 27017
      targetPort: 27017

---

apiVersion: v1
kind: Service
metadata:
  name: paymentservice-mongo
spec:
  selector:
    app: paymentservice-mongo
  ports:
    - port: 27017
      targetPort: 27017
  type: ClusterIP