apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: paymentservice-mongo
spec:
  serviceName: paymentservice-mongo-headless
  replicas: 1
  selector:
    matchLabels:
      app: paymentservice-mongo
  template:
    metadata:
      labels:
        app: paymentservice-mongo
    spec:
      containers:
        - name: mongodb
          image: mongo:7
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_DATABASE
              value: paymentservicedb
            - name: MONGO_INITDB_ROOT_USERNAME
              value: user
            - name: MONGO_INITDB_ROOT_PASSWORD
              value: password
          volumeMounts:
            - name: paymentservice-storage
              mountPath: /data/db
  volumeClaimTemplates:
    - metadata:
        name: paymentservice-storage
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 500Mi
