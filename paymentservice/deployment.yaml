apiVersion: apps/v1
kind: Deployment
metadata:
  name: paymentservice-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: paymentservice-app
  template:
    metadata:
      labels:
        app: paymentservice-app
    spec:
      containers:
        - name: paymentservice-app
          image: paymentservice-app:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: paymentservice-app-configmap
            - secretRef:
                name: paymentservice-app-secret
          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            failureThreshold: 10
            periodSeconds: 60

          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            failureThreshold: 3
            periodSeconds: 15

          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            failureThreshold: 3
            periodSeconds: 10
          resources:
            requests:
              memory: 512Mi
              cpu: 500m
            limits:
              memory: 768Mi
              cpu: 1000m
