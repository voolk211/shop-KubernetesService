apiVersion: v1
kind: Service
metadata:
  name: userservice-postgres-headless
spec:
  clusterIP: None
  selector:
    app: userservice-postgres
  ports:
    - port: 5432
      targetPort: 5432

---

apiVersion: v1
kind: Service
metadata:
  name: userservice-postgres
spec:
  selector:
    app: userservice-postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP