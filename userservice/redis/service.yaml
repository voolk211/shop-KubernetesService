apiVersion: v1
kind: Service
metadata:
  name: userservice-redis-headless
spec:
  clusterIP: None
  selector:
    app: userservice-redis
  ports:
    - port: 6379
      targetPort: 6379

---

apiVersion: v1
kind: Service
metadata:
  name: userservice-redis
spec:
  selector:
    app: userservice-redis
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP
